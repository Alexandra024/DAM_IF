package EX_3EV_ORDINARIA.EjCollections;

// Un Iterator es una forma de recorrer colecciones 
// (como ArrayList, HashSet, etc.) sin necesidad de usar un índice.

// tambien Permite eliminar elementos mientras recorres

        // Iterator<Alumno> it = alumnos.iterator();
        // while (it.hasNext()) {
        //     Alumno a = it.next();
        //     System.out.println(a.getApellido());
        // }

